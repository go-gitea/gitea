rules:
  anonymous-definition:
    disable: true # jobs are identified by id, display names add nothing
  undocumented-permissions:
    disable: true # only non-obvious permissions carry a comment
  concurrency-limits:
    disable: true # cron workflows are serialized by their schedule
