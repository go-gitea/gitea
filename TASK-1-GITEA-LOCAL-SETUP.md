# Task 1 – Gitea Local Setup & Understanding

## Summary

- Cloned and reviewed the Gitea repository and documentation.
- Set up the required development environment on Windows.
- Configured Go, Node.js, and pnpm.
- Downloaded and verified Go dependencies.
- Built the Gitea backend from source.
- Built the frontend using pnpm/Vite.
- Ran Gitea locally without Docker.
- Configured SQLite3 for the local instance.
- Verified Gitea successfully through localhost:3000.

## Issues Resolved

- Resolved a Go module-cache issue during the backend build.
- Resolved the frontend initialization/manifest issue by building the frontend assets.
- Initially encountered a MySQL connection error because MySQL was not running.
- Switched the local setup to SQLite3, avoiding the need for a separate database server.

## What I Learned

Understood the Gitea project structure, backend and frontend build process, dependency management, local configuration, database setup, and how to run and verify Gitea locally without Docker.
