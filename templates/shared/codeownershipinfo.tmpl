{{if .}}
    <div class="file-info-entry">
        <div data-tooltip-content=".locale.Tr "codeowners.owned.by" {{ StringUtils.Join . ", "}} codeowners.codeowners">
            {{svg "octicon-shield-lock"}}
        </div>
    </div>
{{end}}
