<form class="list-header-search ui form ignore-dirty issue-list-search">
	<div class="ui small search fluid action input">
		<input dir="auto" type="hidden" name="type" value="{{$.ViewType}}">
		<input dir="auto" type="hidden" name="state" value="{{$.State}}">
		<input dir="auto" type="hidden" name="labels" value="{{.SelectLabels}}">
		<input dir="auto" type="hidden" name="milestone" value="{{$.MilestoneID}}">
		<input dir="auto" type="hidden" name="project" value="{{$.ProjectID}}">
		<input dir="auto" type="hidden" name="assignee" value="{{$.AssigneeID}}">
		<input dir="auto" type="hidden" name="poster" value="{{$.PosterID}}">
		{{template "shared/searchinput" dict "locale" .locale "Value" .Keyword}}
		{{if .PageIsIssueList}}
			<button id="issue-list-quick-goto" class="ui small icon button gt-hidden" data-tooltip-content="{{.locale.Tr "explore.go_to"}}" data-repo-link="{{.RepoLink}}">{{svg "octicon-hash"}}</button>
		{{end}}
		<button class="ui small icon button" aria-label="{{.locale.Tr "explore.search"}}">{{svg "octicon-search"}}</button>
	</div>
</form>
