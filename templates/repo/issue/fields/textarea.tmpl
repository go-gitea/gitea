<div class="field">
	{{template "repo/issue/fields/header" .}}
	{{/*FIXME: preview markdown */}}
	<textarea name="form-field-{{.ID}}" placeholder="{{.Attributes.placeholder}}" class="edit_area {{if .Attributes.render}}no-easymde{{end}}" {{if .Validations.required}}required{{end}}>{{.Attributes.value}}</textarea>
</div>
