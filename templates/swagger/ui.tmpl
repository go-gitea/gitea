<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<title>Gitea API</title>
		<link href="{{StaticUrlPrefix}}/css/swagger.css?v={{MD5 AppVer}}" rel="stylesheet">
		<style>
			html {
			  box-sizing: border-box;
			  overflow-y: scroll;
			}
			*, *:before, *:after {
			  box-sizing: inherit;
			}
			body {
			  margin: 0;
			  background: #fff;
			}
			.swagger-back-link {
    		  position: absolute;
    		  right: 3rem;
			}
		</style>
	</head>
	<body>
		<h4 class="swagger-back-link"><a href="{{AppUrl}}">{{.i18n.Tr "home"}}</a></h4>
		<div id="swagger-ui" data-source="{{AppUrl}}swagger.{{.APIJSONVersion}}.json"></div>
		<script src="{{StaticUrlPrefix}}/js/swagger.js?v={{MD5 AppVer}}"></script>
	</body>
</html>
