<div class="actions">
	<button class="ui red basic inverted cancel button">
		{{svg "octicon-x"}}
		{{if .ModalNoTranslationKey}}{{.locale.Tr .ModalNoTranslationKey}}{{else}}{{.locale.Tr "modal.no"}}{{end}}
	</button>
	<button class="ui green basic inverted ok button{{if .ExtendedClasses}} {{.ExtendedClasses}}{{end}}" {{if .DataUrl}}data-url="{{.DataUrl}}"{{end}}>
		{{svg "octicon-check"}}
		{{if .ModalYesTranslationKey}}{{.locale.Tr .ModalYesTranslationKey}}{{else}}{{.locale.Tr "modal.yes"}}{{end}}
	</button>
</div>
