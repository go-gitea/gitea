{{$impersonated := ctx.ImpersonatedUser}}
{{if $impersonated}}
<div class="ui warning message impersonate-banner-container">
	<div class="flex-text-block impersonate-banner-content">
		{{svg "octicon-alert"}}
		<span class="tw-flex-1">{{ctx.Locale.Tr "admin.users.impersonating_notice" $impersonated.Name}}</span>
		<a class="ui compact tiny button" href="{{AppSubUrl}}/user/logout">{{ctx.Locale.Tr "admin.users.impersonate_stop"}}</a>
	</div>
</div>
{{end}}
