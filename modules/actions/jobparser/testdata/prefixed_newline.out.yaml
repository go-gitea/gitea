name: Step with leading new line
'on':
  push:
jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - id: extract_tag
        name: "\nExtract tag for variant"
        run: "\necho Test\n"
